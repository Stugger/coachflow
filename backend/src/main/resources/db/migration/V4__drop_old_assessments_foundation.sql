-- Assessment foundation is being removed because assessment movement tracking
-- will be redesigned around programs and persisted training sessions.

DROP TABLE IF EXISTS assessment_template_entries;
DROP TABLE IF EXISTS assessment_templates;
DROP TABLE IF EXISTS assessment_entries;
DROP TABLE IF EXISTS client_assessments;